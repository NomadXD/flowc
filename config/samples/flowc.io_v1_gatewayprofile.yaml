apiVersion: flowc.io.flowc.io/v1
kind: GatewayProfile
metadata:
  labels:
    app.kubernetes.io/name: flowc
    app.kubernetes.io/managed-by: kustomize
  name: gatewayprofile-sample
spec:
  # TODO(user): Add fields here
